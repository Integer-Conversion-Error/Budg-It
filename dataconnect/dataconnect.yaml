specVersion: "v1beta"
serviceId: "budg-it"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "budg-it-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
